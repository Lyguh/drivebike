export interface IContactsCard {
    className: string;
    children: React.ReactNode;
    additional?: string;
    sap?: string;
    text: string;
    description: string;
    information?: string;
}